server {
    server_name ${METUBE_DOMAIN_NAME};

    listen 443 ssl;
    listen [::]:443 ssl;

    location / {
        proxy_pass http://localhost:8096;
    }

    ssl_certificate /etc/letsencrypt/live/${DOMAIN_NAME}/cert.pem;
    ssl_certificate_key /etc/letsencrypt/live/${DOMAIN_NAME}/privkey.pem;
}
